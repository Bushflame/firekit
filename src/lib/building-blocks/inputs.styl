label 
    color var(--beta)
    display block
    font-size 1rem
    //margin .5rem .2rem .5rem

input 
    background transparent
    border 1px solid var(--gamma)
    //border-radius 1.4rem
    color var(--beta)
    display block
    font-size 1rem
    margin auto
    padding  .6rem
    width 18rem //var(--ddWidth)
label 
    color var(--beta)
    //display block
    font-size 1rem
    margin 5px  auto 8px 1rem
    text-align center
textarea 
    background transparent
    border 1px solid var(--beta)
    //border-radius 1.4rem
    color var(--beta)
    //display block
    font-family: Times
    font-size 1.1rem
    line-height 1.4
    margin 8px auto
    padding .6rem
    position relative
    left 50%
    transform translate(-50%)
    //z-index 20
    //width 19rem //var(--ddWidth)